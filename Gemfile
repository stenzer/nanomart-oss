source "http://rubygems.org"

# gem 'highline'
# highline isn't in use, we're not ever actually prompting for age, we can add again when we do

group :test do
  gem 'rake', '~> 10.0'
  gem 'rspec'
end
